apiVersion: v1
kind: Service
metadata:
  name: svc-updateapi
  annotations:
    description: "Servi�� para expor a API de Cria��o para fora do cluster"
spec:
  type: NodePort
  selector:
    app: label-updateapi
  ports:
  - port: 8084
    nodePort: 30003
