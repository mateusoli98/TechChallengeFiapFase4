apiVersion: v1
kind: Service
metadata:
  name: svc-deleteapi
  annotations:
    description: "Servi�� para expor a API de Cria��o para fora do cluster"
spec:
  type: NodePort
  selector:
    app: label-deleteapi
  ports:
  - port: 8082
    nodePort: 30005
    
